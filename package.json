{
  "name": "monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:server": "bun run --filter browser-rpc dev",
    "build:web": "bun run --filter web build && bun run --filter browser-rpc sync-web",
    "build:server": "bun run --filter browser-rpc build",
    "build": "bun run build:web && bun run build:server",
    "start": "bun run --filter browser-rpc start",
    "publish:server": "bun run build && cp README.md packages/server/ && cd packages/server && npm publish --provenance --access public",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@trivago/prettier-plugin-sort-imports": "6.0.2",
    "prettier": "3.7.4",
    "prettier-plugin-tailwindcss": "0.7.2"
  }
}
